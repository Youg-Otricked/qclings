/*
abstract classes are very simple. They are just classes that cannot be instantiated, have static members, or have constructors. They only exist to be inherited from.
*/
int main() {
    `qout("No challenge, just read the lesson");
    return 0;
}
