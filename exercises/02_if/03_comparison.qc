int main() {
/*
QuantumC also has comparison operators.

- > more than
- < less than
- >= at least
- <= at most
- == equals
- != not equals
*/
    int x = 12;
    int y = 21;
    // TODO: create a boolean b thats value is true if x is more than y
    // Don't touch below this line
    `qout("%b", b);
    return 0;
}
