/*
To define a type alias, you use syntax like this.

type MyAlias = Type;

e.g.

type Time = addr_t;

TODO: Define the ErrCode alias, which should be equal to int
*/

ErrCode main() {
    `qout("Done?");
    return 0;
}
